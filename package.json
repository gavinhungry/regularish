{
  "name": "regularish",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild app.jsx --bundle --minify --sourcemap --outfile=app.bundle.js --loader:.js=jsx",
    "serve": "python -m http.server 8080"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
