{
  "name": "regularish",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild app.js --bundle --minify --sourcemap --outfile=app.bundle.js --loader:.js=jsx"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
